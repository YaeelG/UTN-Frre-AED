//! Modelo 1er Parcial AED

/*
* Ejercicio – Análisis de múltiples funciones cuadráticas


    Desarrollar un algoritmo que permita analizar varias funciones cuadráticas de la forma:
*    Y = aX^2 + bX + c

    Para cada función, ingresar los valores de a, b y c, 
    y generar las coordenadas (X,Y) haciendo variar X en el intervalo [20,-20] 
    con decrementos de 2.

    Resolver considerando estas 2 opciones:
        - Opcion 1: El algoritmo deberá solicitar inicialmente la cantidad de funciones a procesar.
        - Opcion 2: cantidad indeterminada de funciones, el usuario indica cuando terminar

    Para cada valor calculado de Y:

        1. Mostrar por pantalla la coordenada correspondiente (X,Y).

        2. Determinar si el valor de Y es múltiplo de 11 aplicando la siguiente propiedad:
            Un número es divisible por 11 cuando la suma de sus dígitos alternos es igual.
            Ejemplo:
            Para el número 5841 --> 5 + 4 = 8 + 1

        3. Informar junto a cada coordenada si el valor de Y es o no múltiplo de 11.

    Al finalizar el procesamiento de cada función, informar:

        - Cantidad de valores de Y múltiplos de 11.
        - Cantidad de valores de Y no múltiplos de 11.
    
    Requisitos
        - Implementar una función que reciba un número y retorne verdadero o falso según sea múltiplo de 11.
        - Implementar al menos un procedimiento para mostrar resultados en pantalla.
        - Considerar valores negativos de Y trabajando con su valor absoluto para verificar divisibilidad.
*/


//* Version profesor de Agus
OPCION 1 
	ESC("Indique la cantidad de funciones que procesaremos!")
	LEER(cant)

	Para i:= 1 hasta cant hacer 
	
		ESC("Indiquelos coeficientes A, B y C de la ecuancion cuadratica")
		LEER(a,b,c)
		MultNo:=0
		MultSi:=0
		Para x:= 20 hasta -20, -2 hacer 
			// Punto 1			
			y:= a* (x**2) + b*x + c
			(20;23)
			//Punto 2 
			suma1:=0
			suma2:=0
			pos:= 1 - 2 - 3 - 4 - 5
			// y= 5841 - 584 - 58 - 5 - 0
			Mientras y > 0 hacer 
				Si pos MOD 2 = 1 entonces 
					suma1:= suma1 + Y MOD 10   / 9
				sino 
					suma2:= suma2 + Y MOD 10  / 5
				FinSi 
				pos:= pos  + 1
				Y:= Y DIV 10 
			FinMientras 
			
			Si suma1 = suma2 entonces 
				MultSi:= MultSi + 1
				ESC("(",x,";",y,") - Es multiplo de 11")            
			sino 
				MultNo:= MultNo + 1
				ESC("(",x,";",y,") - No es multiplo de 11")            
			FinSi 
		FinPara
		ESC("para la funcion procesada tenemos:")
		ESC(MultNo,"valores de Y que no son multiplos de 11")
		ESC(MultSi,"valores de Y que si son multiplos de 11")
	FinPara



OPCION 2 
	ESC("Desea procesar una funcion cuadratica? SI - NO")
	LEER(Rta)
	
	Mientras Rta = "SI" hacer 
	
		// logica de tratamiento de la funcion! 
		
		
		fin//
		
		ESC("Desea procesar una nueva funcion cuadratica? SI - NO")
		LEER(Rta)
	FinMientras


//* Versión de clase, corregida por profesores

//Esta demaciado segmentado en procedimientos y funciones. 
//En general hay mucho que se puede mejorar. No usar ciertas constantes en las funciones

Accion Funcion_cuadrat_mult11 es
    AMBIENTE
        A, B, C: entero
        x, y: entero
        op: (0, 1, 2)
        num_funcion: entero //contador de funcion
        continuar: ("s", "n")

        cant_ym11: entero
        cant_Ynom11: entero


        Funcion mult_11  (n: entero): logico es
            AMBIENTE
                invertir = -1 //al multiplicar ppor opeación cambia el signo
                operacion: entero
                sum: entero
            PROCESO
                multi_11 := falso
                operación := 1 //inicializo operacion en positivo???
                sum := 0

                REPETIR
                    sum := sum + n mod 10 * operacion
                    operacion := operacion * invertir
                    n := n div 10
                HASTA QUE (n=0)

                si sum = 0 entonces
                    multi_11 := verdadero
                finsi
        Fin_Funcion

        Procedimiento Imp_X_Y es
                Escribir("x=", x, "y=", y)
                
                Si multi_11(y) entonces
                    Escribir("Y es multiplo de 11")
                    cant_ym11 := cant_ym11 + 1
                Sino
                    Escribir("Y no es múltiplo de 11")
                    cant_Ynom11 := cant_Ynom11 + 1
                fin_si
        Fin_Procedimiento

        Procedimiento Recorrido_funcion es
            cant_ym11 := 0
            cant_Ynom11 := 0

            Esc("Ingrese los coeficientes a,b y c de la forma ax^2 + bx + c = 0")
            LEER(a, b, c)

            Para x := 20 hasta (-20), (-2) hacer
                y := a*(x**2) + b*x + c
                Imp_X_Y
            fin_Para

            Si a > max_a entonces
                max_a := a
            fin_si
            escribir("Cantidad de Y multiplos de 11:", cant_ym11)
            escribir("Cantidad de Y multiplos de 11:", cant_Ynom11)
        Fin_Procedimiento

        Procedimiento proc_def es
            esc("Ingrese cuántas funciones desean operar:")
            Leer(num_func)

            Para i:=1 hasta num_func hacer
                Escribir("Trabajando función", i, "de", num_func)
                Recorrido_funcion
            fin_Para
        Fin_Procedimiento
            

    PROCESO
         Escribir("Se generaran las coordenadas", x, y, "para las funciones cuaadráticas ingresadas")

         //Menú de opciones
         Escribir("Ingrese el número de opción que necesite:
                    Op1: Cantidad definida de funciones
                    Op1: Cantidad indeterminada de funciones")

        Leer(op)
        max_a := 0

        SEGUN op HACER
            1: proc_def
            2: prod_indef
            otro: Esc("Opción Incorrecta")
        FIN_SEGUN

        Escribir("El mayor número 'a' fue", max_a)
Fin_Accion


//* Otra version de clase
Accion MultiFcCuadratica es
    AMBIENTE
        //Completar

        Funcion mult11(nro: entero): logico es
            AMBIENTE
                i, dig, sumap, sumaimp: entero  //suma pares e impares

            PROCESO
                sumap := 0
                sumaimp := 0
                i := 1

                Mientras nro > 0 hacer 
                    dig := nro mod 10
                    
                    Si i mod 2 = 0 entonces
                        sumap := sumap + dig
                    Sino
                        sumaimp := sumaimp + dig
                    fin_si

                    nro := nro div 10
                Fin_Mientras

                
